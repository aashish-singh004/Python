4th of july. 

For loop - Loops allows us to tell the computer to repeat actions without having to write the repeated code. Something like what i learned in linux. For example-

fruits = [ "Apple" , "Peach" , "Pear"]

for fruit in fruits:

    print(fruit)    #indentation is very important

There are so many inbuilt functions like to get highest no. in variable use, max or to to get of the variable , use sum but i can get highest no. wihout using max, haha just learnt

student_score = [ "1" , "2", "3","4"]
highest_score = 0

for score in highest_score:

    if score > highest score:
       
        highest score = score

       print(highest_score) #the output will be 4, highest no.

For loops with range - The combination of the range() function with the python for loop allows us to run a loop for as many times as we wish. Instead of looping through each item in a list, we can loop through a range of numbers.

Range Function

range(1, 10)

This code doesn't do anything by itself. For example- if i tried to print it, it would not give me individual numbers.
But we can conjucate it with for loops like

for number in range(1,10):

print ("number") # output will be 1,2,3,4,5,6,7,8,9, not 10 , also if we do range(1,11,3) - the output will be 1,4,7,10 as we changed the stepsize to 3 , by default its
+1  but now+3


while i created a password generator, i used a command like .join() which is instered inside a variable like password= "".join(password) it joined all the strings and made
a single string. 
    
     


